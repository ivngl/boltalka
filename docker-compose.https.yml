services:
  nginx:
    ports:
      - "443:443"
    volumes:
      - ./nginx/default.https.conf:/etc/nginx/conf.d/default.conf:ro
      - /root/certs:/certs:ro
  app:
    environment:
      URL_PROTOCOL: https
