{
  "name": "boltalka",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci --omit=dev && npm run build && cd ../server && npm ci --omit=dev && npx prisma generate",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
